import Tabs from "./tabs";

const Constants = {
    tabs: Tabs,
    default: {
        tabs: {
            invoice: "allInvoice",
            client: "allClients"
        }
    }
}

export default Constants